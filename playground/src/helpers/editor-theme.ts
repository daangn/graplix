import { atomoneInit } from "@uiw/codemirror-theme-atomone";

export const editorTheme = atomoneInit({
  settings: {
    background: "#171717",
    gutterBackground: "#171717",
    gutterActiveForeground: "#d4d4d4",
    lineHighlight: "#202020",
  },
});
